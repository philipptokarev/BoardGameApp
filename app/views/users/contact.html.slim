.row
  p
  | You will contact to
  '
  = user.email
  | .
  '
  | User will get notifiction on his email.
  = simple_form_for @message, url: send_msg_path do |f|
    = f.input :name, required: true

    = f.input :email, as: :email, required: true

    = f.input :text, required: true

    = f.input :user_id, as: :hidden

    = f.submit
